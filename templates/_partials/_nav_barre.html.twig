


<div class="sticky top-0 z-50">
    <nav class="bg-white border-b border-gray-100 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-24 items-center relative">
                
                <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
                    <a href="{{ path('app_campagne_index') }}" class="pointer-events-auto text-2xl font-black tracking-tight text-teal-600 hover:text-teal-700 transition-colors">
                        Cagnot'Potes
                    </a>
                </div>

                <div class="flex items-center md:hidden">
                    <input type="checkbox" id="menu-toggle" class="peer hidden" />
                    
                    <label for="menu-toggle" class="inline-flex items-center justify-center p-2 rounded-md text-gray-500 hover:text-teal-600 hover:bg-gray-50 cursor-pointer transition-colors select-none">
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                        </svg>
                    </label>

                    <div class="hidden peer-checked:flex flex-col absolute top-16 left-0 w-full bg-white border-b border-gray-100 shadow-xl px-4 py-4 space-y-3 z-50">
                        <a href="{{ path('app_campagne_new') }}" class="block w-full text-center bg-pink-500 hover:bg-pink-600 text-white text-sm font-semibold px-4 py-3 rounded shadow transition-colors uppercase tracking-wider">
                            Créer une campagne
                        </a>
                    </div>
                </div>

                <div class="hidden md:flex md:items-center md:space-x-4 ml-auto z-10">
                    <a href="{{ path('app_campagne_new') }}" class="bg-pink-500 hover:bg-pink-600 text-white text-sm font-semibold px-4 py-2.5 rounded shadow transition-colors uppercase tracking-wider">
                        Créer une campagne
                    </a>
                </div>

            </div>
        </div>
    </nav>
</div>







{# 
    <div class="sticky top-0 z-50">
    <nav class="bg-white border-b border-gray-100 shadow-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center relative">
                
                <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
                    <a href="{{ path('app_campagne_index') }}" class="pointer-events-auto text-2xl font-black tracking-tight text-teal-600 hover:text-teal-700 transition-colors">
                        Cagnot'Potes
                    </a>
                </div>

                <div class="flex items-center md:hidden">
                    <button type="button" id="mobile-menu-button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-500 hover:text-teal-600 hover:bg-gray-50 focus:outline-none transition-colors">
                        <span class="sr-only">Ouvrir le menu</span>
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
                        </svg>
                    </button>
                </div>

                <div class="hidden md:flex md:items-center md:space-x-4 ml-auto z-10">
                    <a href="{{ path('app_campagne_new') }}" class="bg-pink-500 hover:bg-pink-600 text-white text-sm font-semibold px-4 py-2.5 rounded shadow transition-colors uppercase tracking-wider">
                        Créer une campagne
                    </a>
                </div>

            </div>
        </div>

        <div id="mobile-menu" class="hidden md:hidden border-t border-gray-100 bg-white">
            <div class="px-2 pt-2 pb-4 space-y-1 shadow-inner">
                <a href="{{ path('app_campagne_new') }}" class="block w-full text-center bg-pink-500 hover:bg-pink-600 text-white text-sm font-semibold px-4 py-3 rounded shadow transition-colors uppercase tracking-wider">
                    Créer une campagne
                </a>
            </div>
        </div>
    </nav>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        const button = document.getElementById('mobile-menu-button');
        const menu = document.getElementById('mobile-menu');

        if (button && menu) {
            button.addEventListener('click', function() {
                menu.classList.toggle('hidden');
            });
        }
    });
</script> #}